git add .
git commit -m "open source"

### crear branch main 
git branch -M main


git push -u origin main

### precompile and push directo a cloudflare, sin pasar por github

npm run precompile && npm run build && wrangler deploy

git add .
git commit -m " fixes"
git push origin main

npx wrangler dev